from setuptools import setup, find_packages

setup(
    name="custom_utils",
    version="0.1.0",
    packages=find_packages(),
    description="A library of helpful utilities for CustomProj",
    python_requires='>=3.6',
)